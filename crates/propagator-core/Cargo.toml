[package]
name = "propagator-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Stochastic cellular wildfire spread simulator (Rust core, ported from the Python/numba implementation)"

[dependencies]
# intentionally dependency-free: std-only core
