[target.wasm32-unknown-unknown]
# getrandom 0.3 (pulled in by Bevy's web event stack) requires an explicit
# opt-in to the browser Web Crypto backend.
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
