/target
/results
/dist

# Overpass response cache — 8.8 MB, purely a network cache. Deleting it just
# forces scripts/fetch_osm.py to refetch.
data/osm_raw.json

# Inspection-only duplicate of spotorno_render_terrain.f32, regenerated by
# scripts/build_render_terrain.py. The .f32 is what the game actually reads.
data/spotorno_render_terrain.tif

.DS_Store
*.pyc
__pycache__/
tools/mcp/.venv/
